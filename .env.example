# Postgres connection string used by Prisma
DATABASE_URL=

# Cloudinary credentials for /api/upload
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# HTTP Basic credentials guarding /admin and every write endpoint.
# Both must be set, otherwise those routes reject all requests.
ADMIN_USERNAME=
ADMIN_PASSWORD=
